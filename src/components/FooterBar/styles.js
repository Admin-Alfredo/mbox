import styled from "styled-components";

export const Container = styled.div`
  width: 100%;
  position: fixed;
  bottom: 0;
  left: 0;
  display: flex;
  justify-content: space-around;
  background-color: var(--bg-color);
  border-top: 1px solid  var(--bg-component-gray);;
  padding: 5px 0px;
`